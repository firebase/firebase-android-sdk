plugins {
    id 'firebase-library'
}

firebaseLibrary {
    publishSources = true
    publishJavadoc = true
}

android.compileSdkVersion = 26
android.sourceSets {
    main.java.srcDirs += 'src/main/kotlin'
}