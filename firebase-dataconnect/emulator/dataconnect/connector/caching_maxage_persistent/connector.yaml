connectorId: caching_maxage_persistent
authMode: PUBLIC
generate:
  kotlinSdk:
    outputDir: ../../.generated/caching_maxage_persistent
    package: com.google.firebase.dataconnect.connectors.caching_maxage_persistent
    clientCache:
      maxAge: 1h
      storage: persistent

